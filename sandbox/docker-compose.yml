services:
  sandbox-os:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    ports:
      - "6080:6080"  # noVNC WebSocket
      - "5900:5900"  # VNC TCP
    volumes:
      - ./shared:/root/shared
      - ./mcp-servers:/opt/mcp-servers  # Mount MCP servers for development
